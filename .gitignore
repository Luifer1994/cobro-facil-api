# Ignorar directorios de dependencias
/vendor
/node_modules

# Ignorar archivos y directorios de compilación
/public/build
/public/hot
/public/storage

# Ignorar todo dentro de storage
/storage/*

# Pero excluir (no ignorar) ciertos subdirectorios dentro de storage
!/storage/app
!/storage/app/public
!/storage/logs
!/storage/pail

# Ignorar archivos específicos dentro de storage
/storage/*.key

# Ignorar otros archivos y directorios no necesarios
/.phpunit.cache
/.fleet
/.idea
/.vscode
/.zed
composer.lock
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log

# Ignorar directorio de lenguaje si es necesario (opcional)
# /lang

# Opcional: Mantener archivos vacíos en los directorios que se incluyen
# Para que Git rastree los directorios, puedes agregar un archivo vacío llamado .gitkeep en cada uno de ellos.
# Por ejemplo:
# /storage/app/.gitkeep
# /storage/app/public/.gitkeep
# /storage/logs/.gitkeep
# /storage/pail/.gitkeep
